{
  "name": "animood",
  "version": "0.1.0",
  "private": true,
  "description": "AniMood — emotional anime/manga/manhwa discovery platform (monorepo root)",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.20.0 <21",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
