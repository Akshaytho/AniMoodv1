import { Stub } from '@/components/Stub';

export default function CommunityPage() {
  return (
    <Stub
      title="Community"
      description="Discussions seeded from real AniList and MAL threads, paraphrased and emotionally-tagged. Talk about anime by feeling, not just plot."
      upcomingIn="Phase 4 (post-launch)"
      currentPath="/community"
    />
  );
}
