import { Stub } from '@/components/Stub';

export default function SavedPage() {
  return (
    <Stub
      title="Saved"
      description="Titles you've bookmarked from anywhere on AniMood — works without an account via anonymous cookie."
      upcomingIn="Phase 4 (post-launch)"
      currentPath="/saved"
    />
  );
}
