import { Stub } from '@/components/Stub';

export default function MapPage() {
  return (
    <Stub
      title="Emotional Map"
      description="Full-screen force-directed graph of every emotion, theme, character, and title. Drag, zoom, and traverse the relationships."
      upcomingIn="commit 17"
      currentPath="/map"
    />
  );
}
