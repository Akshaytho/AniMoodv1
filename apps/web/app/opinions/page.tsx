import { Stub } from '@/components/Stub';

export default function OpinionsPage() {
  return (
    <Stub
      title="Opinions Spectrum"
      description="Where the fanbase agrees and where it splits — by title, broken into story tension, character depth, emotional impact, and themes."
      upcomingIn="commit 18"
      currentPath="/opinions"
    />
  );
}
